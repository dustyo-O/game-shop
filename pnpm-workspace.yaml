packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency lifecycle scripts by default. Only these are allowed
# to run; each one installs or verifies a native binary it cannot work without.
onlyBuiltDependencies:
  - esbuild
