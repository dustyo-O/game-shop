# Tasks: Failure and Recovery

- **Spec:** `003-failure-and-recovery`
- **Functional:** `functional-spec.md` — 9 requirements, 38 acceptance criteria
- **Technical:** `technical-considerations.md` — 12 sections, 12 risks, 10 assumptions

> **Standing requirement.** Every slice ends with a task explaining to the user how that feature works and its role among the key points of the test task. This is a deliverable, not a courtesy: the assignment grades «способность объяснить решения».
>
> **Two things every slice must honour**, both carried from the tech spec's risk table:
> - **R2 — assert stock accounting, not the shopper's key count.** Phase 2's RED validation came back green because `deliveries.order_id UNIQUE` masked a weakened lock. The same masking applies here. `count(*) FROM supplier_keys WHERE claimed_by_request_id IS NOT NULL` must equal `count(*) FROM deliveries`. That is §2.2's fifth criterion and, for most of this phase, the only assertion that can fail.
> - **Verification runs across separate API processes.** A `max: 1` pool serialises everything inside one instance, so a single-instance check passes against a broken implementation (`architecture.md` §7: 20 distinct keys against 1 process, 9 against 4).

---

- [x] **Slice 1: A delivery that failed is a state the shopper can see**

  > The smallest end-to-end increment: the status exists, the shop can reach it, and a shopper reads an honest explanation. Nothing retries yet.

  - [x] Write the migration for this slice: widen `orders_status_check` to admit `'delivery_failed'`. Follow `technical-considerations.md` §10's measured ordering and its note on why `text` + CHECK was chosen over a Postgres enum **for exactly this migration**. Do not weaken the CHECK to make ordering easier. **[Agent: postgres-drizzle-database]**
  - [x] Add `delivery_failed` to `packages/contracts/src/order-status.ts`, in **`recoverableOrderStatuses`, not `terminalOrderStatuses`** — terminal is I9's set, and putting it there makes the operator retry illegal by construction. Let `_EveryOrderStatusIsClassified` fire first and fix it deliberately. Rewrite the doc comments that say "Phase 3" in the future tense. **[Agent: nestjs-backend]**
  - [x] Add `markDeliveryFailed` (`delivering → delivery_failed`) to `order-transitions.ts`. No new SQL and no new service method — the file's header predicts this is a data-table edit. Confirm `_NoTransitionLeavesATerminalState` still compiles. **[Agent: nestjs-backend]**
  - [x] Add the Russian label and the recovery explanation. **Sequence this deliberately:** add the contracts member first and watch **both** tripwires fire — the total `Record<OrderStatus, string>` at compile time and `apps/api/test/unit/order-status-russian-labels.test.ts` under `pnpm test`. That is the RED validation for this string and it costs nothing. Then add `order-recovery-explanation.ts` (total over the recoverable set) and render it on the order page. Text per `technical-considerations.md` §9.2 — both explanations open with «Оплата прошла» and differ in exactly the clause §2.3's second criterion asks about. **[Agent: vanilla-ts-frontend]**
  - [x] Verify: drive an order into `out_of_stock` and into `delivery_failed`, and confirm in a real browser that each renders its own explanation, that the two differ, and that a reload shows the same text rather than an error. Assert `data-status` and `data-order-recovery` rather than the sentences. Keep the two screenshots under `docs/screenshots/` following the existing naming; delete any other artifacts. **[Agent: testing-expert]**
  - [x] Explain to the user how this concrete feature works and what its role is among the key points of the test task in particular — here: why `delivery_failed` had to be recoverable rather than terminal, and what the two compile-time tripwires bought. **[Agent: nestjs-backend]**

- [x] **Slice 2: A supplier that refuses is replaced by the backup**

  > Settles §2.1. The ladder's first two rungs — `askFirst` and `fallThrough` — and the machinery to make a supplier refuse on demand.

  - [x] Add the migration for the attempt ledger: `attempt` and `probe_count` columns (both `NOT NULL DEFAULT 1`, then `DROP DEFAULT` — §10 explains why leaving the default is a silent-reuse bug), the two `CHECK`s, **`UNIQUE (order_id, attempt)`** (not the three-column version — `attempt` is numbered per order, so `(x,a,3)` and `(x,b,3)` must collide), `supplier_requests.provider`, and drop the now-redundant `issuance_attempts_order_id_idx`. Run §10's pre-flight query before migrating. **[Agent: postgres-drizzle-database]**
  - [x] Add the `supplier_behaviour` table and its control endpoint `PUT /internal/suppliers/:provider/behaviour`, behind the existing `AdminTokenGuard`. Behaviour lives in the database, not process memory — `pnpm race` runs four processes and an in-process rate reaches none of the others. The one-shot `fail_next`/`hang_next` counters decrement via an atomic conditional UPDATE in I7's shape. **[Agent: nestjs-backend]**
  - [x] Add supplier B: `apps/api/src/suppliers/b/`, mirroring `a/` and exporting nothing, calling the same `SupplierKeyClaimService`. Generalise `supplier-a.client.ts` → `supplier.client.ts` taking `provider` and config. Add `SUPPLIER_B_CONFIG` through the same `readUrl` validator — **the `SUPPLIER_A_URL` scheme trap applies verbatim** (`localhost:3000/...` parses cleanly as scheme `localhost:` and fails only inside `fetch`, at the first fall-through, on a paid order). Carry A's `.env.example` warning across. **[Agent: nestjs-backend]**
  - [x] Add `supplier_rejected` to `SupplierIssueErrorReason` and extend `transitionForReason`. An injected refusal must not reuse `out_of_stock` — that would be a lie whenever the pool is full and routes to the wrong order status. `4xx`, never `5xx`: answered, and the answer is no. Let `assertNever` force the mapping. **[Agent: nestjs-backend]**
  - [x] Build `issuance-ladder.ts` with the `askFirst`, `fallThrough` and `settleRefused` rungs, and `issuance-runner.service.ts` as the single entry point. **`IssuanceHistory.readWithin(tx: Transaction, …)` takes a `Transaction` and no overload for the pooled handle** — reading the ladder's inputs outside the lock must not compile (R4). Annotate the emitted SQL. **[Agent: nestjs-backend]**
  - [x] Wire **refusal** injection into both stub controllers: consume `fail_next`, then `failure_rate`, before the key claim, answering `supplier_rejected`. Use `SupplierBehaviourService.consumeOneShot` — it exists, is atomic, and has no caller. _Added mid-slice: slice 2 task 2's brief deferred all injection to slice 3, which left this slice's own verification unsatisfiable — `fail_next` round-trips through the endpoint and nothing reads it. Only the **hang** placement belongs to slice 3, because it must sit **after** the claim commits so the ledger holds a code for the re-probe to find; a refusal has no such constraint and is this slice's subject._ **[Agent: nestjs-backend]**
  - [x] Verify across **four API processes**: set A's `fail_next = 1`, buy, and confirm the order reaches `delivered` with exactly two attempt rows (`a/1 failed`, `b/2 ok`), one delivery, one `supplier_requests` row, and **claimed keys equal to deliveries**. Then delete any temporary scripts. **[Agent: testing-expert]**
  - [x] Explain to the user how this concrete feature works and what its role is among the key points of the test task in particular — here: why the fall-through mints a new request id while a re-probe must not, and why the two suppliers share one key pool. **[Agent: nestjs-backend]**

- [ ] **Slice 3: Silence is investigated, never assumed to be failure**

  > The requirement the whole phase turns on — §2.2. The `probe` rung, the `unknown` guard, and a documentation error that would have made this slice's own check vacuous.

  - [x] **First, correct the inequality that is documented backwards** in `architecture.md` §5, `apps/api/src/config/supplier-config.ts` and `.env.example`. All three state `injected hang < SUPPLIER_TIMEOUT_MS`, which produces no timeout at all — the trap this slice exists to demonstrate never fires and the check passes having exercised nothing. It must read `SUPPLIER_TIMEOUT_MS < hang_ms < function ceiling`, and the reasoning given for the old direction conflates the *client* giving up (`AbortSignal.timeout` aborts the socket, not the remote handler) with the *platform* killing the function. Record both hang scenarios separately: short-before-the-claim (a slow supplier is not a failed one) and long-after-the-claim (the trap). **[Agent: general-purpose]**
  - [x] Place the injected hang **after** the key claim commits in both stubs, so the ledger holds a code for the re-probe to find. A hang before the claim exercises the re-probe path but not the double-issue trap; both are worth having and they are different checks. **[Agent: nestjs-backend]**
  - [x] Narrow the ledger read to `WHERE request_id = $1 AND provider = $2`. `supplier_requests.provider` was added and is written in slice 2, but `readLedger` still filters on `request_id` alone — and its own doc comment already claims the predicate shipped, so the comment describes code that is not there. Not a live defect today (derived ids carry the provider segment, so no path can address a lookup to the wrong supplier), but **the re-probe is what makes this read load-bearing**: a mis-addressed probe must return zero rows rather than the other supplier's code. **[Agent: nestjs-backend]**
  - [ ] Add the `probe` rung and `settleNeverEstablished` to the ladder, with `SUPPLIER_MAX_PROBES_PER_REQUEST` (default 3, per A1). The probe reuses the **same three arguments** to `deriveIssuanceRequestId` and therefore recomputes a byte-identical id — nothing is remembered and nothing is read back. Probes are counted in `probe_count` via the narrow `ON CONFLICT DO UPDATE` that touches that column **and nothing else** (resetting `status` would erase the one fact worth having). Annotate the emitted SQL. **[Agent: nestjs-backend]**
  - [ ] Enforce the hard rule: **never fall through while any attempt for this order is `unknown`.** `settleNeverEstablished` outranks `fallThrough` in the decision order, so the rule is unrepresentable rather than merely obeyed. On that path write **nothing** to `issuance_attempts` — the row already says `unknown` with `last_error` NULL, and that *is* the record §2.2's fourth criterion asks for. **[Agent: nestjs-backend]**
  - [ ] Log the computed worst-case invocation budget at boot beside the existing supplier line (`probes × timeout × providers`), and document the inequality in `.env.example`. Defaults give 12s, which exceeds Vercel's Hobby ceiling — nothing can enforce this in code, so it must be visible (R5). Move `SHUTDOWN_DRAIN_TIMEOUT_MS` with the budget. **[Agent: vercel-infra]**
  - [ ] Verify across four processes: A `hang_next = 1` with `hang_ms > SUPPLIER_TIMEOUT_MS`, hang after the claim. Confirm the order reaches `delivered` with **one** attempt row (`a/1`, `status = 'ok'`, `probe_count = 2`), **one** `supplier_requests` row, **one** claimed key — and that supplier B was never called. Then run the RED: weaken the **ladder rule** (let `fallThrough` fire while an attempt is `unknown`) and confirm **stock accounting** fails while the shopper's key count stays at one. Restore byte-identical and report the hash. Delete temporary scripts. **[Agent: testing-expert]**
  - [ ] Explain to the user how this concrete feature works and what its role is among the key points of the test task in particular — here: why a timeout is not a failure, why re-asking the same supplier is safe while asking a different one is not, and why the RED had to assert stock accounting rather than the shopper's key count. **[Agent: nestjs-backend]**

- [ ] **Slice 4: A person can find every purchase that was paid for but never delivered**

  > Settles §2.4. The list is a query, not a queue.

  - [ ] Add `payment_events_paid_order_idx` and `orders_undelivered_idx` per `technical-considerations.md` §5, with the measured before/after in the migration comment. Note the caveat beside the partial index: a later phase adding another undelivered status must rebuild it, and the failure mode is not an error — it is the new status silently falling out of the operator's list. **[Agent: postgres-drizzle-database]**
  - [ ] Build `UndeliveredOrdersService` and `GET /api/admin/orders/undelivered` behind the existing guard. Use §4's exact statement: `LEFT JOIN LATERAL … LIMIT 1` (a plain join returns 3 198 rows for 1 599 orders; `CROSS JOIN LATERAL` silently *drops* orders with no attempt row, which are the most alarming ones on the screen), `NOT EXISTS` on `deliveries` (not redundant with the status filter — `issuance.service.ts` has a live path that leaves a delivered order reading `delivering`), and the status list as **literals, not `= ANY($1)`** (the one place this codebase's own bound-parameter convention must not be copied; a bound array defeats the partial index under a generic plan). Surface `outstanding_request_id`, and **never the shopper's key**. Re-check `admin.module.ts`'s module-distance argument after adding imports. Annotate the emitted SQL. **[Agent: nestjs-backend]**
  - [ ] Build the operator's view at `/admin/recovery` — functional and unstyled, seven view states as a discriminated union with `assertNever`. **No client-side route guard** — a check the browser makes is a check the browser can be told to skip; the page renders for anyone and every byte of data comes from a guarded endpoint. Token in `sessionStorage` (deliberately the opposite choice from `purchase-intent.ts`, and every access in `try`/`catch` because the accessor itself throws when site data is blocked), **never `VITE_ADMIN_TOKEN`** — the admin page and the storefront are one bundle. The `401`/`503` split mirrors the guard's own answers; `503` shows no form, because pasting cannot help. **[Agent: vanilla-ts-frontend]**
  - [ ] Verify: with orders in `paid`, `delivering`, `out_of_stock`, `delivery_failed` and `delivered`, confirm the first four are listed and the fifth is not; confirm a delivered-but-`delivering` order is excluded by the `NOT EXISTS`; confirm the empty state says so in words rather than showing a blank screen; and confirm the endpoint answers `401` with no token, `401` with a wrong one, and `503` with `ADMIN_TOKEN` unset — asserted against the endpoint, never by inspecting the DOM. Then delete any artifacts. **[Agent: testing-expert]**
  - [ ] Explain to the user how this concrete feature works and what its role is among the key points of the test task in particular — here: why the expensive part of the query was `paid_at` rather than the per-order-latest-row problem everyone looks at, and why "paid but undelivered" is a wider set than "stuck". **[Agent: nestjs-backend]**

- [ ] **Slice 5: An operator can push a stuck order through, and pressing twice changes nothing**

  > Settles §2.5 and closes the assignment's fourth adversarial scenario. This is where the order lock stops being defence in depth.

  - [ ] Add `retryIssuance` (`out_of_stock, delivery_failed → delivering`) and `resumeIssuance` (`delivering → delivering`) to `order-transitions.ts`. **Keep `resumeIssuance` as its own row rather than widening `retryIssuance`'s `from` list** — its guard matches every time, so its exclusion is the row lock and the fact that every concurrent resumer necessarily computes the same `probe` of the same outstanding id. That is R3, it is the phase's most delicate decision, and the table is where a reviewer will look for it. **[Agent: nestjs-backend]**
  - [ ] Build `POST /api/admin/orders/:orderId/retry` calling `runner.runForOrder(orderId, IssuanceEntry.Operator)` — the **identical** claim-under-lock and ladder the automatic path uses, with no admin-only code path into issuance. `200` reports the outcome; `409` means *this order is not stuck*, decided by zero rows from the guarded UPDATE rather than an `if`. Add a comment naming automatic scheduled retrying as a deliberate omission (R11) — the obvious "improvement" is a timer, and a timer against a failing supplier is how a small outage becomes a large one. **[Agent: nestjs-backend]**
  - [ ] Add the retry button — per-row busy state, never global; every outcome re-fetches the whole list because the endpoint is the authority; an unanswered retry reads as *"may or may not have run — refresh to see"* rather than as a failure, which is this phase's own thesis applied on the client. Manual refresh only, no timer. **[Agent: vanilla-ts-frontend]**
  - [ ] Verify across four processes: restock after an empty pool and retry, confirming exactly one key; retry the same order repeatedly; and **fire two concurrent retries at one order from different processes**. Assert against the database — one delivery, one claimed key, **claimed keys equal to deliveries**. This must **not** be a double-click test: a check that clicks Retry twice in one browser proves only that the button was disabled, cannot fail against a broken server, and therefore cannot pass either. Delete temporary scripts. **[Agent: testing-expert]**
  - [ ] Explain to the user how this concrete feature works and what its role is among the key points of the test task in particular — here: how the retry reuses Phase 2's exclusions rather than inventing its own, and what `resumeIssuance` rests on now that its guard excludes nobody. **[Agent: nestjs-backend]**

- [ ] **Slice 6: The shopper watches recovery happen**

  > Settles §2.6. A criterion that fails by definition rather than by bug if this slice is skipped.

  - [ ] Split the order page's stop condition: stop on `isTerminalOrderStatus`, keep reading recoverable states at a slower cadence, and **snap back to 1s the moment a read shows the order back in flight** — at a 5s beat a 25–65ms issuance is invisible, so the page would jump from «Не удалось выдать ключ» straight to «Ключ выдан», a quiet regression of spec 002 §2.5 caused by a change that never mentions it. Leaving `isSettledOrderStatus` compiles, ships, keeps every existing check green, and leaves §2.6's third criterion silently unmet (R10). Amend the four doc comments listed in `technical-considerations.md` §9.1 in the same commit — a stale tripwire is worse than none. **[Agent: vanilla-ts-frontend]**
  - [ ] Verify in a real browser: leave a shopper's page open and untouched on a stuck order, retry from a **second context**, and confirm the key appears with no interaction. Then run the RED: restore `isSettledOrderStatus` as the stop condition and confirm this check fails. Restore and confirm byte-identical. Delete any recordings. **[Agent: testing-expert]**
  - [ ] Explain to the user how this concrete feature works and what its role is among the key points of the test task in particular — here: why "settled" and "terminal" had to stop being the same question, and how a criterion can fail by definition while every test stays green. **[Agent: vanilla-ts-frontend]**

- [ ] **Slice 7: The reviewer runs the recovery checks**

  > Settles §2.7. Three named checks alongside the existing five.

  - [ ] Write `recover:refusal`, `recover:timeout` and `recover:out-of-stock` in `scripts/race/`, auto-discovered by `pnpm race`. Use **only** one-shot `fail_next`/`hang_next` counters and rates of exactly 0 or 1 — a fractional rate makes §2.7's fifth criterion untrue by construction (R8). Assert the database as well as the response, and restore the seeded baseline. Add the per-check npm aliases. **[Agent: testing-expert]**
  - [ ] Apply RED validation to all three: weaken the mechanism each defends, confirm the failure, restore byte-identical, report the hashes, and add them to `scripts/race/README.md`'s existing RED table. Remember R2 — for `recover:timeout` the assertion that can fail is stock accounting, not the shopper's key count. **[Agent: nestjs-backend]**
  - [ ] Verify: run the whole set twice with no tidying between, confirm 8/8 both times, and confirm the new checks skip or run honestly against a single external base URL. Delete temporary artifacts. **[Agent: testing-expert]**
  - [ ] Explain to the user how this concrete feature works and what its role is among the key points of the test task in particular — here: which of the assignment's five adversarial scenarios are now settled and runnable, and what each new check's RED result actually proves. **[Agent: nestjs-backend]**

- [ ] **Slice 8: The walkthrough of what was built**

  > §2.8. A deliverable of the phase, not documentation overhead.

  - [ ] Write `docs/walkthrough/phase-3.md` covering the three required keystones: why an unanswered request is not a failed one; why asking the same supplier again is safe while asking a different one is not; and how a purchase can be recovered long after it went wrong. Each entry states the decision, the more obvious alternative, what breaks without it, and — where a guarantee is enforced by the records — the exact statement and what zero returned rows means. Match the existing phase walkthroughs and cite the per-slice ones rather than re-deriving them. **[Agent: nestjs-backend]**
  - [ ] Review it against §2.8's criteria in two passes: first reading **only** the walkthrough, standing in for a reader who has never seen the source, listing every term that carries load without being explained; then opening the source to fact-check its SQL, constraint names and numbers. Revise what genuinely fails a criterion; do not rewrite for taste. **[Agent: general-purpose]**
  - [ ] Explain to the user how this concrete feature works and what its role is among the key points of the test task in particular — here: how Phase 3's keystones complete the argument Phases 1 and 2 built, and which interview question each answers. Update `docs/walkthrough/phases-1-and-2.md` into `phases-1-to-3.md`, or extend it, so the question→answer index stays the single map. **[Agent: general-purpose]**

- [ ] **Slice 9: Feature Testing & Regression**

  > Verifies the whole feature end-to-end against functional-spec.md, run after all implementation slices are complete.
  - [ ] Read functional-spec.md acceptance criteria in full. Generate acceptance-level tests that verify the entire feature as a whole — not individual slices. Cover applicable layers (unit for pure logic, integration for service interactions, e2e for user flows) based on the project's testing stack. Annotate each test with `@spec: 003-failure-and-recovery` and `@regression` if suitable for long-term regression. **[Agent: testing-expert]**
    - _On RED validation here: the implementation is already complete, so "write it failing first" is not available. Demonstrate instead that each test **can** fail by a method that does not modify production source — inverting an expectation, or pointing the assertion at a scenario the shop genuinely does not satisfy. Slices 3, 6 and 7 already establish RED by weakening production code; do not repeat that here._
    - _Mark honestly what is not testable at this layer rather than covering it with an adjacent assertion. A coverage table whose every row says "covered" is a table nobody checked._
  - [ ] Run all generated tests. All must pass. Fix any failures before proceeding. **[Agent: testing-expert]**
  - [ ] Explain to the user how this concrete feature works and what its role is among the key points of the test task in particular — here: what the suite covers, what it deliberately does not, and where each uncovered criterion was actually verified. **[Agent: nestjs-backend]**
