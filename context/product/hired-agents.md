# Specialist Agents Coverage

Generated by `/awos:hire` on 2026-09-02. Re-run `/awos:hire` to refresh — this file goes stale as soon as `.claude/agents/` or `context/product/architecture.md` changes.

## Coverage by Technology

| Technology | Recommended Subagent Role | Status | Agent |
| ---------- | ------------------------- | ------ | ----- |
| TypeScript | `nestjs-backend` / `vanilla-ts-frontend` | ✅ Covered | nestjs-backend, vanilla-ts-frontend |
| NestJS | `nestjs-backend` | ⚠️ Partial — no NestJS skill in registry | nestjs-backend |
| Node.js 22 | `nestjs-backend` | ✅ Covered | nestjs-backend |
| REST API design | `nestjs-backend` | ✅ Covered | nestjs-backend |
| Pino (logging) | `nestjs-backend` | ⚠️ Partial — no logging skill in registry | nestjs-backend |
| Vite | `vanilla-ts-frontend` | ⚠️ Partial — no Vite skill in registry | vanilla-ts-frontend |
| Vanilla DOM / CSS | `vanilla-ts-frontend` | ✅ Covered | vanilla-ts-frontend |
| FSD-inspired layering | `vanilla-ts-frontend` | ⚠️ Partial — skill is React-specific | vanilla-ts-frontend |
| PostgreSQL 16 | `postgres-drizzle-database` | ✅ Covered | postgres-drizzle-database |
| Drizzle ORM | `postgres-drizzle-database` | ⚠️ Partial — no Drizzle skill in registry | postgres-drizzle-database |
| drizzle-kit (migrations) | `postgres-drizzle-database` | ⚠️ Partial — no Drizzle skill in registry | postgres-drizzle-database |
| Neon (hosted Postgres) | `postgres-drizzle-database` | ⚠️ Partial — no Neon skill in registry | postgres-drizzle-database |
| Vercel (serverless) | `vercel-infra` | ⚠️ Partial — no Vercel skill in registry | vercel-infra |
| Docker Compose | `vercel-infra` | ⚠️ Partial — no Docker skill in registry | vercel-infra |
| pnpm workspaces | `vercel-infra` | ⚠️ Partial — no pnpm skill in registry | vercel-infra |
| Vitest | `qa-testing` | ⚠️ Partial — no Vitest skill in registry | testing-expert |
| Playwright | `qa-testing` | ✅ Covered | testing-expert |
| Race / recovery scripts | `qa-testing` | ✅ Covered | testing-expert |

## Registered Specialist Subagents

| Name | Description | Skills |
| ---- | ----------- | ------ |
| testing-expert | Feature-level QA agent for AWOS projects. Analyzes functional-spec.md acceptance criteria and generates comprehensive acceptance tests (unit, integration, e2e) that verify the entire feature works as described. Called at the end of feature development via the Feature Testing & Regression slice in tasks.md — not per-slice. Supports RED validation and annotates tests with @spec and @regression for regression suite management. | verify-ui |
| nestjs-backend | Use for backend work in apps/api — NestJS modules, controllers and providers, REST endpoints, the order lifecycle, payment webhook intake, supplier issuance, promo redemption, and anything touching the correctness invariants in context/product/architecture.md §3. | typescript-development |
| postgres-drizzle-database | Use for schema design, Drizzle queries, drizzle-kit migrations, seeds, connection/pooling configuration, and any statement involving locks, unique constraints, conditional updates or transaction boundaries. | postgres-best-practices, typescript-development |
| vanilla-ts-frontend | Use for all work in apps/web — the storefront markup and CSS, the five required interactions (banner carousel, catalog menu, currency toggle, service hover, card hover), the order status page, the admin view, and the API client. | typescript-development, react-feature-sliced-design, verify-ui |
| vercel-infra | Use for the pnpm workspace layout, Docker Compose local stack, Vercel deployment and function configuration, Neon provisioning, environment variables, and the npm scripts that run the app and the race checks. | — |

## Installed Hooks

None installed.

## Gaps

- NestJS: no framework-specific skill in the registry → `nestjs-backend` works from general knowledge; the architecture's §3/§3.1 invariants are cited in its agent file to compensate.
- Drizzle ORM and drizzle-kit: no skill in the registry → `postgres-drizzle-database` works from general knowledge; the project's raw-SQL annotation rule is written into its agent file.
- Neon: no skill in the registry → serverless connection rules (pool size 1, prepared statements disabled) are written into the agent files rather than supplied by a skill.
- Vercel, Docker Compose, pnpm workspaces: no skills in the registry → `vercel-infra` works from general knowledge.
- Vite: no skill in the registry → `vanilla-ts-frontend` works from general knowledge.
- Vitest: no skill in the registry → `testing-expert` covers Playwright via `verify-ui`, but unit/integration test patterns come from general knowledge.
- FSD layering: `react-feature-sliced-design` is React-specific while this frontend is vanilla TS → the agent file instructs using it for layer discipline only and ignoring React-specific guidance.
- Pino / structured logging: no skill in the registry → correlation-id requirements are written into `nestjs-backend`.
